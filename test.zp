(load "cli/cli")
(load "minitest/minitest")

(import-all "cli")
(import-all "minitest")

(set! zepto:args '("first" 1))

(minitest:assert-equal
  1
  (begin
    (cli:create (list (cli:command "first")))
    (cli:play))
  "assert that we can get arguments")
